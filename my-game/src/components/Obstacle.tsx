import React from "react";

interface ObstacleProps{
    x: number;
    y: number;
    width: number;
    height: number;
}

const Obstacle:React.FC<ObstacleProps> = ({x,y,width,height}) => {
    return null;
};

export default Obstacle;